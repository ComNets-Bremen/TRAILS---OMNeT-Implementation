package inet.mobility.trails;
import inet.mobility.base.MovingMobilityBase;
simple TRAILS extends MovingMobilityBase
{
    parameters:
		bool initFromDisplayString = default(true);
        @class(TRAILS);
}